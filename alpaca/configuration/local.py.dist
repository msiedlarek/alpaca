from alpaca.configuration.base import *

DEBUG = False

SECRET_KEY = ''

MONGODB_CONNECTIONS = {
    'default': {
        'name': 'alpaca',
        'host': 'localhost',
        'username': None,
        'password': None,
    },
}

# Mapping of reporter identifiers to their API keys
REPORTERS = {}

LOG_FILE = None
